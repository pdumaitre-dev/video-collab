generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Video {
  id              Int       @id @default(autoincrement())
  title           String
  description     String?
  sourceUrl       String
  durationSeconds Float?
  createdAt       DateTime  @default(now())

  comments Comment[]
}

model Comment {
  id           Int      @id @default(autoincrement())
  videoId      Int
  video        Video    @relation(fields: [videoId], references: [id], onDelete: Cascade)
  startSeconds Float
  endSeconds   Float
  text         String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model Comment_blob {
  id           Int      @id @default(autoincrement())
  pathname     String   // Blob pathname, e.g. "videos/sample.mp4"
  startSeconds Float
  endSeconds   Float
  text         String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([pathname])
}

