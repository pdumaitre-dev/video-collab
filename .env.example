# Copy to .env and fill in values (or inject via your shell / Cursor Cloud secrets).

# Neon dashboard → Connection string (pooled recommended)
DATABASE_URL=

# Vercel project → Storage → Blob → Read-write token
BLOB_READ_WRITE_TOKEN=

# optional: private (default) | public
# BLOB_ACCESS=private
